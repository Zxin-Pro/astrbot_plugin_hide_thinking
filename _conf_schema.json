{
  "enabled": {
    "description": "启用插件",
    "type": "bool",
    "hint": "关闭后不再隐藏思考内容。",
    "default": true
  },
  "strip_tags": {
    "description": "删除思维链标签",
    "type": "bool",
    "hint": "删除 <think> / <thinking> / <reasoning> 等标签及其内部思考，只留正文。",
    "default": true
  },
  "strip_injected": {
    "description": "删除已拼进正文的思考块",
    "type": "bool",
    "hint": "删除回复开头的「🤔 思考:」整段（AstrBot 开启显示思考内容时会拼进去）。",
    "default": true
  },
  "strip_special": {
    "description": "删除结束符",
    "type": "bool",
    "hint": "删除漏进正文的 <|eos|>、<|im_end|>、</s> 等模型结束符。",
    "default": true
  },
  "collapse_duplicate": {
    "description": "折叠整段复读",
    "type": "bool",
    "hint": "同一句话被粘两遍时只留一句；分段发出后，后一条把前一条接在尾巴上也会剪掉。",
    "default": true
  },
  "strip_tool_call": {
    "description": "删除泄漏的工具调用",
    "type": "bool",
    "hint": "模型把 <tool_call> / send_message_to_user 当正文发出时，抽出里面的人话，外壳丢掉。",
    "default": true
  },
  "dedup_window": {
    "description": "跨条去重等待窗口（秒）",
    "type": "int",
    "hint": "同一句话两种空格写法先后到达时，只发空格更自然的那条；0 关闭缓冲立即发送。建议大于分段回复的间隔秒数。",
    "default": 2
  }
}
